# Dashboard

TrailScope uses an Inertia + React dashboard.

Roles:

- `support`: journey status, trace status, and step messages.
- `developer`: sanitized technical context and stack traces.
- `admin`: developer visibility plus TrailScope user management.

Basic auth is not supported. Use TrailScope users, host gate mode, or signed URLs.
